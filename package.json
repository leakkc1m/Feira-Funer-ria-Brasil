{
  "name": "feira-fun-ria-screens",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:sync": "browser-sync start --server --files \"index.html,**/*.css,**/*.js\" --host 0.0.0.0 --port 3000 --no-open"
  },
  "dependencies": {
    "puppeteer": "^24.34.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}